#!/bin/bash

tools=( 
    github.com/shoenig/petrify
    github.com/alecthomas/gometalinter
    github.com/golang/dep/cmd/dep
    )

for i in "${tools[@]}"; do
    echo "Updating and installing $i"
    go get -v -u $i && go install $i/...
    echo "$i installed"
done
